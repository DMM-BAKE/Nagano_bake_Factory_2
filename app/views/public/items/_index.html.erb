<div class="items">
  <% items.each do |item| %>
    <div class="item-content">
      <%= image_tag item.get_image(400, 400) %>
        <%= link_to item.name, public_item_path(item.id), class: "item-price" %>
        ¥<%= item.price %>
    </div>
  <% end %>
