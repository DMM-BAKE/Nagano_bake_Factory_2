<div class="items">
  <% items.each do |item| %>
    <div class="item-content">
      <%= image_tag item.get_image(100, 100) %>
      <%= link_to item.name, public_item_path(item.id) %>
      <%= item.price %>
    </div>
  <% end %>
