<div class="genrekara mt-5" style="font-size :20px;" >
  ジャンルから探す
</div>
  <table>
    <tr>
      <td>
        <%= link_to "全商品を表示", public_items_path, style: "text-decoration:none;" %>
      </td>
    </tr>
    <% genres.each do |genre| %>
    <%# if genre.is_genres_status == true %>
      <tr>
        <td>
          <%= link_to genre.name, public_genres_path(genre.id), style: "text-decoration:none;"%>
          </td>
      </tr>
         <% end %>
    <%# end %>
  </table>